apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nuclio

resources:
  - certificate.yaml
  - reference-grant.yaml
  - httproute.yaml

components:
  - ../../components/crowdsec-bouncer
