# TODO: Replace with actual sealed values generated by kubeseal
#
# Generate with:
#   kubectl create secret generic openclaw-secret \
#     -n openclaw \
#     --from-literal=OPENCLAW_GATEWAY_TOKEN=<your-token> \
#     --dry-run=client -o yaml | \
#     kubeseal --controller-name=sealed-secrets-controller \
#              --controller-namespace=kube-system \
#              --format yaml > k8s/apps/openclaw/sealedsecret.yaml
#
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: openclaw-secret
  namespace: openclaw
spec:
  encryptedData:
    OPENCLAW_GATEWAY_TOKEN: "<REPLACE_WITH_SEALED_VALUE>"
  template:
    metadata:
      name: openclaw-secret
      namespace: openclaw
    type: Opaque
