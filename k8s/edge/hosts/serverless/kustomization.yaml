apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - certificate.yaml
  - reference-grant.yaml
  - httproute.yaml
  - middleware.yaml

patches:
  - path: listener.yaml
    target:
      group: gateway.networking.k8s.io
      version: v1
      kind: Gateway
      name: public
